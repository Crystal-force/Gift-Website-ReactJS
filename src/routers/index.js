import { ConfirmExchange, CreateExchange, ExchangeInfomation, TouchedExchange, WishListPage } from "../pages";

const router = [
  {
    path: '/create-exchange',
    component: CreateExchange,
  }
]

export default router;